/** Client wait budget for director-calibration (multi-step Claude pipeline). */
export const DIRECTOR_CALIBRATION_TIMEOUT_MS = 210_000;

/** Default edge-function timeout used by other tabs (alignment, assembly, etc.). */
export const DEFAULT_EDGE_FUNCTION_TIMEOUT_MS = 90_000;

/** Alignment / optimize-bullet client timeout — unchanged from prior behavior. */
export const ALIGNMENT_TIMEOUT_MS = 120_000;
